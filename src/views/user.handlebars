<div>
    <h1>Tu perfil</h1>
    <p>{{user.name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>
    <p>{{user.userRole}}</p>
    <a href="/products"><button>seguir comprando</button></a>
    <a href="/api/sessions/logout">Cerrar session</a>
</div>